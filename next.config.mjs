/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
         hostname: "supportal-alphaib.kimsoreya.site",
      },
    ],
  },
};

export default nextConfig;
